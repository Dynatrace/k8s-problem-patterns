apiVersion: apps/v1
kind: Deployment
metadata:
  name: exceeded-quota-demo
  namespace: exceeded-quota
spec:
  selector:
    matchLabels:
      demo: exceeded-quota
  replicas: 3
  template:
    metadata:
      labels:
        demo: exceeded-quota
    spec:
      containers:
      - name: tomcat
        image: tomcat
        resources:
          requests:
            memory: 100Mi